query EnumeratePack($id: ID!) {
  enumeratePack(id: $id) {
    globals {
      ...GlobalPythonModuleFull
    }
    models {
      ...DataModelFull
    }
    detections {
      ... on Rule {
        ...RuleSummary
      }

      ... on Policy {
        ...PolicySummary
      }
    }
  }
}
